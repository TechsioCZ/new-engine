{
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "name": "smart-suggest-validation",
  "projectType": "library",
  "sourceRoot": "libs/smart-suggest/validation/src",
  "tags": ["type:lib", "scope:smart-suggest", "platform:agnostic"],
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "cwd": "libs/smart-suggest/validation",
        "command": "pnpm run build"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/smart-suggest/validation",
        "command": "pnpm run typecheck"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "libs/smart-suggest/validation",
        "command": "pnpm run test"
      }
    }
  }
}
