x-medusa-env: &medusa-env
  JWT_SECRET: ${DC_JWT_SECRET:-supersecret}
  COOKIE_SECRET: ${DC_COOKIE_SECRET:-supersecret}
  DATABASE_TYPE: postgres
  DATABASE_URL: ${DC_DATABASE_URL}
  LEGACY_DATABASE_URL: ${DC_LEGACY_DATABASE_URL}
  MINIO_FILE_URL: ${DC_MINIO_FILE_URL}
  MINIO_REGION: ${DC_MINIO_REGION}
  MINIO_ENDPOINT: ${DC_MINIO_ENDPOINT}
  MINIO_BUCKET: ${DC_MINIO_BUCKET}
  MINIO_ACCESS_KEY: ${DC_MINIO_ACCESS_KEY}
  MINIO_SECRET_KEY: ${DC_MINIO_SECRET_KEY}
  REDIS_URL: ${DC_REDIS_URL}
  MEILISEARCH_HOST: ${DC_MEILISEARCH_HOST}
  MEILISEARCH_API_KEY: ${DC_MEILISEARCH_API_KEY}
  STORE_CORS: ${DC_STORE_CORS}
  ADMIN_CORS: ${DC_ADMIN_CORS}
  AUTH_CORS: ${DC_AUTH_CORS}
  SENTRY_NAME: ${DC_SENTRY_NAME}
  SENTRY_DSN: ${DC_SENTRY_DSN}
  SETTINGS_ENCRYPTION_KEY: ${DC_SETTINGS_ENCRYPTION_KEY}
  FEATURE_PPL_ENABLED: ${DC_FEATURE_PPL_ENABLED:-0}
  PPL_ENVIRONMENT: ${DC_PPL_ENVIRONMENT:-testing}
  IS_IFRAME_PAYLOAD: ${DC_IS_IFRAME_PAYLOAD}
  PAYLOAD_BASE_URL: ${DC_PAYLOAD_BASE_URL}
  PAYLOAD_IFRAME_URL: ${DC_PAYLOAD_IFRAME_URL}
  PAYLOAD_API_KEY: ${DC_PAYLOAD_API_KEY}
  FEATURE_PAYLOAD_ENABLED: ${DC_FEATURE_PAYLOAD_ENABLED:-0}
  CMS_CACHE_TTL: ${DC_CMS_CACHE_TTL:-3600}
  CMS_LIST_CACHE_TTL: ${DC_CMS_LIST_CACHE_TTL:-600}
  PAYLOAD_WEBHOOK_SECRET: ${DC_PAYLOAD_WEBHOOK_SECRET}
  PAYLOAD_SSO_PRIVATE_KEY: ${DC_PAYLOAD_SSO_PRIVATE_KEY}
  PAYLOAD_SSO_USER_EMAIL: ${DC_PAYLOAD_SSO_USER_EMAIL}
  PAYLOAD_SSO_ISSUER: ${DC_PAYLOAD_SSO_ISSUER}
  PAYLOAD_SSO_AUDIENCE: ${DC_PAYLOAD_SSO_AUDIENCE}
  PAYLOAD_SSO_ALG: ${DC_PAYLOAD_SSO_ALG}
  PAYLOAD_SSO_TOKEN_TTL: ${DC_PAYLOAD_SSO_TOKEN_TTL}

x-medusa-base: &medusa-base
  restart: unless-stopped
  container_name: wr_medusa_be
  ports:
    - "9000:9000"
    - "9009:9009"
    - "24678:24678"
  depends_on:
    - medusa-db
    - medusa-valkey
    - medusa-minio
    - medusa-meilisearch
  networks:
    - internal
  environment:
    <<: *medusa-env

services:
  # DEV MODE (default): docker compose up
  # PROD MODE: docker compose -f docker-compose.yaml -f docker-compose.prod.yaml up --build
  medusa-be:
    <<: *medusa-base
    build:
      context: .
      dockerfile: docker/development/medusa-be/Dockerfile
      target: dev
      args:
        FEATURE_PPL_ENABLED: ${DC_FEATURE_PPL_ENABLED:-0}
        FEATURE_PAYLOAD_ENABLED: ${DC_FEATURE_PAYLOAD_ENABLED:-0}
    volumes:
      - .:/var/www
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/health"]
      start_period: 10s
      interval: 30s
  payload:
    restart: unless-stopped
    container_name: wr_payload
    build:
      context: .
      dockerfile: docker/development/pnpm/Dockerfile
    working_dir: /var/www
    command: >-
      sh -c "pnpm --filter payload payload migrate &&
      pnpm --filter=payload run dev --hostname 0.0.0.0 --port 8083"
    volumes:
      - .:/var/www
    ports:
      - "8083:8083"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8083/api/health"]
      start_period: 20s
      interval: 30s
      timeout: 10s
      retries: 5
    depends_on:
      - medusa-db
    networks:
      - internal
    environment:
      DATABASE_URL: ${DC_PAYLOAD_DATABASE_URL}
      PAYLOAD_SECRET: ${DC_PAYLOAD_SECRET}
      PAYLOAD_SCHEMA_NAME: ${DC_PAYLOAD_DATABASE_SCHEMA_NAME}
      PAYLOAD_BASE_URL: ${DC_PAYLOAD_BASE_URL}
      MEDUSA_BACKEND_URL: ${DC_FE_MEDUSA_BACKEND_URL}
      FEATURE_PAYLOAD_ARTICLES_ENABLED: ${DC_FEATURE_PAYLOAD_ARTICLES_ENABLED:-0}
      FEATURE_PAYLOAD_PAGES_ENABLED: ${DC_FEATURE_PAYLOAD_PAGES_ENABLED:-0}
      FEATURE_PAYLOAD_HERO_CAROUSELS_ENABLED: ${DC_FEATURE_PAYLOAD_HERO_CAROUSELS_ENABLED:-0}
      PAYLOAD_WEBHOOK_SECRET: ${DC_PAYLOAD_WEBHOOK_SECRET}
      PAYLOAD_LOCALES: ${DC_PAYLOAD_LOCALES}
      PAYLOAD_SSO_PUBLIC_KEY: ${DC_PAYLOAD_SSO_PUBLIC_KEY}
      PAYLOAD_SSO_ISSUER: ${DC_PAYLOAD_SSO_ISSUER}
      PAYLOAD_SSO_AUDIENCE: ${DC_PAYLOAD_SSO_AUDIENCE}
      PAYLOAD_SSO_ALG: ${DC_PAYLOAD_SSO_ALG}
      PAYLOAD_SSO_ALLOWED_ORIGINS: ${DC_PAYLOAD_SSO_ALLOWED_ORIGINS}
      OPENAI_API_KEY: ${DC_OPENAI_API_KEY}
      # OPENAI_BASE_URL: ${DC_OPENAI_BASE_URL}
      S3_ENDPOINT: ${DC_PAYLOAD_S3_ENDPOINT}
      S3_REGION: ${DC_PAYLOAD_S3_REGION}
      S3_BUCKET: ${DC_PAYLOAD_S3_BUCKET}
      S3_ACCESS_KEY_ID: ${DC_PAYLOAD_S3_ACCESS_KEY_ID}
      S3_SECRET_ACCESS_KEY: ${DC_PAYLOAD_S3_SECRET_ACCESS_KEY}
      NODE_ENV: development
  medusa-valkey:
    image: valkey/valkey:8-alpine
    healthcheck:
      test: "[ $$(valkey-cli ping) = 'PONG' ]"
      start_period: 5s
      timeout: 3s
      interval: 1s
      retries: 5
    ports:
      - "6379:6379"
    volumes:
      - medusa-valkey-data:/data
    networks:
      - internal
  medusa-minio:
    image: minio/minio
    container_name: wr_medusa_minio
    ports:
      - "9003:9003"
      - "9004:9004"
    volumes:
      - medusa-minio-data:/data
    networks:
      - internal
    command: server /data --console-address ":9003" --address :9004
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9003"]
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
  medusa-meilisearch:
    image: getmeili/meilisearch:v1.20
    container_name: wr_medusa_meilisearch
    ports:
      - "7700:7700"
    volumes:
      - medusa-meilisearch-data:/meili_data
    networks:
      - internal
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:7700"]
      interval: 10s
      timeout: 5s
      retries: 5
    environment:
      MEILI_MASTER_KEY: ${DC_MEILI_MASTER_KEY}
      MEILI_NO_ANALYTICS: true
  medusa-db:
    image: postgres:17.6-alpine
    restart: unless-stopped
    command: ["postgres", "-cshared_preload_libraries=pg_stat_statements"]
    ports:
      - "5432:5432"
    volumes:
      - ${DC_POSTGRES_VOLUME_DATA:-./.docker_data/db}:/var/lib/postgresql/data
    networks:
      - internal
    healthcheck:
      test: ["CMD", "pg_isready", "-d", "medusa"]
      start_period: 10s
      interval: 30s
      timeout: 60s
      retries: 3
    environment:
      POSTGRES_PASSWORD: root
      POSTGRES_USER: root
      POSTGRES_DB: medusa
      PGDATA: "/var/lib/postgresql/data"
  engine-db:
    image: mariadb:latest
    restart: no
    environment:
      MYSQL_ROOT_PASSWORD: 1234
      MYSQL_DATABASE: wrshop
      MYSQL_USER: wruser
      MYSQL_PASSWORD: 1234
    command: --sql_mode="NO_ENGINE_SUBSTITUTION"
    ports:
      - "3306:3306"
    volumes:
      - ./.docker_data/dbmysql:/var/lib/mysql
      - ./docker/development/mariadb/wr.cnf:/etc/mysql/conf.d/wr.cnf
    networks:
      - internal
  adminer:
    image: adminer
    restart: unless-stopped
    ports:
      - "8081:8080"
    depends_on:
      - medusa-db
    networks:
      - internal
    environment:
      ADMINER_DESIGN: nette
      ADMINER_DEFAULT_SERVER: medusa-db
      ADMINER_PLUGINS: "dump-json"
  caddy:
    build:
      context: ./docker/development/caddy
      dockerfile: Dockerfile
    cap_add:
      - NET_ADMIN
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    networks:
      - internal
    volumes:
      - ./docker/development/caddy/Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    extra_hosts:
      - host.docker.internal:host-gateway
networks:
  internal:
    driver: bridge

volumes:
  medusa-valkey-data:
  medusa-minio-data:
  medusa-meilisearch-data:
  caddy_data:
  caddy_config:
