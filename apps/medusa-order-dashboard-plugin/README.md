# Medusa Order Dashboard Plugin

Internal Medusa v2 plugin for the unified order operations dashboard admin UI route.

The plugin currently owns only Admin customizations. It calls the existing admin API
endpoints exposed by `apps/medusa-be`.
