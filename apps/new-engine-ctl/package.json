{
  "name": "new-engine-ctl",
  "version": "0.0.1",
  "private": true,
  "bin": {
    "new-engine-ctl": "./src/cli.ts"
  },
  "type": "module",
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "dev": "nub src/cli.ts",
    "lint": "ultracite check src scripts",
    "format": "oxfmt src scripts",
    "test": "vitest run",
    "typecheck": "pnpm -w exec tsc --noEmit -p scripts/typescript/projects/apps/new-engine-ctl/tsconfig.json"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "yaml": "^2.9.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.12.4",
    "esbuild": "^0.28.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": "24.x"
  }
}
