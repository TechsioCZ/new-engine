# Tooling preferences

- Expects the coding agent to have Figma MCP access configured for design-to-code work, and actively follows concrete setup steps (e.g., `cmd mcp add`/`auth`) when access is missing — reporting errors back for further troubleshooting. Confidence: 0.7
- Works on Windows, runs the `cmd` CLI from Git Bash, and has PowerShell available for diagnostics. Confidence: 0.8
